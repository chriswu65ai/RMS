services:
  promptmanager:
    build: .
    ports:
      - "4173:4173"
    environment:
      - SQLITE_PATH=/data/promptmanager.db
    volumes:
      - promptmanager_data:/data

volumes:
  promptmanager_data:
