services:
  researchmanager:
    build: .
    ports:
      - "4173:4173"
    environment:
      - SQLITE_PATH=/data/researchmanager.db
    volumes:
      - researchmanager_data:/data

volumes:
  researchmanager_data:
